(()=>{'use strict';const slides=[...document.querySelectorAll('.slide')],dots=document.getElementById('dots'),count=document.getElementById('count'),bar=document.getElementById('bar'),loader=document.getElementById('loader'),head=document.querySelector('body>header'),up=document.getElementById('up'),down=document.getElementById('down'),cursor=document.getElementById('cursor'),ring=document.getElementById('ring'),reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;let active=0,wheelLock=false;slides.forEach((s,i)=>{const b=document.createElement('button');b.type='button';b.setAttribute('aria-label',`انتقل إلى ${s.dataset.title||`الشريحة ${i+1}`}`);b.onclick=()=>go(i);dots.appendChild(b)});const nav=[...dots.children];function set(i){active=Math.max(0,Math.min(slides.length-1,i));slides.forEach((s,n)=>s.classList.toggle('active',n===active));nav.forEach((b,n)=>b.classList.toggle('active',n===active));count.textContent=String(active+1).padStart(2,'0');bar.style.height=`${(active+1)/slides.length*100}%`;head.classList.toggle('glass',active>0);up.disabled=active===0;down.disabled=active===slides.length-1}function go(i){const t=slides[Math.max(0,Math.min(slides.length-1,i))];t&&t.scrollIntoView({behavior:reduce?'auto':'smooth',block:'start'})}const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting&&e.intersectionRatio>=.55)set(slides.indexOf(e.target))}),{threshold:[.35,.55,.75]});slides.forEach(s=>observer.observe(s));addEventListener('keydown',e=>{if(['ArrowDown','PageDown',' '].includes(e.key)){e.preventDefault();go(active+1)}if(['ArrowUp','PageUp'].includes(e.key)){e.preventDefault();go(active-1)}if(e.key==='Home')go(0);if(e.key==='End')go(slides.length-1)});addEventListener('wheel',e=>{if(innerWidth<1080||reduce||wheelLock||Math.abs(e.deltaY)<24)return;wheelLock=true;go(active+(e.deltaY>0?1:-1));setTimeout(()=>wheelLock=false,780)},{passive:true});up.onclick=()=>go(active-1);down.onclick=()=>go(active+1);document.querySelectorAll('[data-next]').forEach(b=>b.onclick=()=>go(active+1));if(matchMedia('(pointer:fine)').matches){let mx=innerWidth/2,my=innerHeight/2,rx=mx,ry=my;addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;cursor.style.transform=`translate(${mx}px,${my}px) translate(-50%,-50%)`});(function tick(){rx+=(mx-rx)*.16;ry+=(my-ry)*.16;ring.style.transform=`translate(${rx}px,${ry}px) translate(-50%,-50%)`;requestAnimationFrame(tick)})();document.querySelectorAll('button,a,article,.chips i').forEach(el=>{el.addEventListener('mouseenter',()=>ring.classList.add('hot'));el.addEventListener('mouseleave',()=>ring.classList.remove('hot'))})}const hero=document.querySelector('.hero'),scene=document.querySelector('.scene');if(hero&&scene&&!reduce){hero.addEventListener('mousemove',e=>{const r=hero.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;scene.style.transform=`translate3d(${x*-12}px,${y*-10}px,0)`});hero.addEventListener('mouseleave',()=>scene.style.transform='translate3d(0,0,0)')}addEventListener('load',()=>setTimeout(()=>loader.classList.add('hide'),reduce?80:850));set(0)})();